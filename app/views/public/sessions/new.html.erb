<div class="container">
  <h5 class="bg-light col-2 my-5 text-center">ログイン</h5>
  <div class="container">
    <p class="bg-light text-center col-3">会員の方はこちらからログイン</p>
    <div class="container">
      <%= form_with model: @customer, url: customer_session_path, class: "form-group" do |f| %>
      
        <div class="form-group">
            <%= f.label :email, "メールアドレス", class: "form-label" %>
            <%= f.email_field :email, autofocus: true, autocomplete: "email", placeholder: "sample@example.com", class: "form-control" %>
        </div>
  
        <div class="form-group">
          <%= f.label :password, "パスワード", class: "form-label" %>
          <%= f.password_field :password, autocomplete: "current-password", class: "form-control" %>
        </div>
  
        <div class="actions text-center">
          <%= f.submit "ログイン", class: "btn btn-primary col-2 my-3" %>
        </div>
      <% end %>
    </div>
  </div>
  <div class="container"> 
    <div class="bg-light text-center col-3">会員登録がお済みでない方</div>
    <div class="row my-3 justify-content-center">
      <div class="col-6">
        <%= link_to "こちら", new_registration_path(resource_name) %>から新規登録を行ってください。
      </div>
    </div>
  </div>
</div>
